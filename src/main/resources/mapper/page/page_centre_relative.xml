<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace='page_centre_relative'>

    <select id="find" parameterType="java.util.HashMap" resultType="java.util.HashMap" useCache="false">


    </select>


    <insert id="insert" parameterType="java.util.HashMap">
        INSERT INTO page_centre_relative(id,page_id,relative_page_id) VALUES
        (#{id,jdbcType=VARCHAR},#{page_id,jdbcType=VARCHAR},#{relative_page_id,jdbcType=VARCHAR});
    </insert>

    <delete id="delete" parameterType="java.util.HashMap">
        DELETE FROM page_centre_relative WHERE page_id=#{page_id,jdbcType=INTEGER} and relative_page_id=#{relative_page_id,jdbcType=INTEGER}
    </delete>

</mapper>
